/**
 * 配置信息
 */
export class Profile {

    /*
     $result['basedata'] = $baseData;
     $result['productunit'] = $productunit;
     $result['authitems'] = $authItems;
     //$result['nicklist'] = Employee::model()->getNickNameList();
     $result['systemconfig'] = $systemConfigList['rows'];
     $result['xmpp'] = Yii::app()->session ->get('xmpp');
     */
    /**
    * 缓存
    */
    public static instance: any;
}
